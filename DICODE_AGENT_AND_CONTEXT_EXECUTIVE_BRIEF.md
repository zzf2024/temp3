# DiCode Agent 核心架构改造汇报

## 摘要

本次改造的核心，不是提升某一次模型回答，而是加强模型之外的两层系统能力：

- **Agent Harness（运行框架）**：负责驱动任务、执行工具、处理错误、判断进展、恢复循环并保存状态；
- **Context Engineering（上下文工程）**：负责决定模型在每一轮应该看到什么、保留什么、压缩什么，以及何时注入提醒和记忆。

```text
                 DiCode Agent
                      |
        +-------------+-------------+
        |                           |
        v                           v
+------------------+      +----------------------+
| Agent Harness    |      | Context Engineering  |
| 任务如何运行     |<---->| 模型每轮看到什么     |
+------------------+      +----------------------+
        |                           |
        +-------------+-------------+
                      v
                模型 + 工具
```

这两个层面共同决定 Agent 是否可靠。模型能力再强，如果运行框架不能把错误反馈回来，Agent 仍会重复；上下文窗口再大，如果信息组织失控，Agent 仍会遗忘目标。

本次工作完成了 Agent 恢复、工具错误闭环、运行证据、长短期记忆、上下文压缩、环境降噪和两条测评管线建设。其中，上下文压缩经历 9 个阶段的设计、实现和验收；Agent 测评固定了 50 个主测案例；当前 Agent 聚焦回归 95 项通过，上下文工程收束回归 6308 项通过。

## 1. 为什么要改造模型之外的系统

传统方式通常把 Agent 简化为：

```text
用户问题 -> 模型 -> 工具 -> 模型 -> 最终回答
```

真实长任务远比这复杂。系统必须持续处理：

- 工具参数是否有效；
- 工具结果是否完整反馈；
- 任务是否真正推进；
- 历史是否已经超过模型承载能力；
- 哪些旧信息可以删除，哪些必须精确保留；
- 中断后如何恢复；
- Agent 宣称完成是否可信；
- 问题发生后能否还原现场。

因此，生产级 Agent 的主体不是一次模型调用，而是一套围绕模型持续运行的工程系统。

## 2. DiCode 总体架构

```text
用户输入
   |
   v
+------------------- 输入与任务入口 -------------------+
| 新任务 | 恢复任务 | 后续指令 | 用户确认 | 取消       |
+--------------------------+---------------------------+
                           |
                           v
+-------------------- Agent Harness --------------------+
| 任务生命周期                                           |
| 模型请求循环 -> 工具解析 -> 工具执行 -> 结果反馈       |
| 进展判断 -> 错误恢复 -> 完成判断 -> 状态持久化         |
+--------------------------+---------------------------+
                           |
                每次模型请求前/后协同
                           |
                           v
+---------------- Context Engineering ------------------+
| 用量计算 | 环境增量 | 短期状态 | 长期记忆召回          |
| 工具输出裁剪 | 近期对话 | 结构化摘要 | 关键状态保护    |
| 恢复提醒 | 完整性检查 | 压缩防抖 | 安全回退          |
+--------------------------+---------------------------+
                           |
             +-------------+-------------+
             |                           |
             v                           v
       +------------+              +------------+
       | 模型服务   |              | 工具系统   |
       +------------+              +------------+
             |                           |
             +-------------+-------------+
                           v
+-------------------- 证据与测评 -----------------------+
| 模型历史 | 工具参数与结果 | 生命周期 | 状态检查         |
| Agent 基准评测 | 上下文同输入对比 | 真实任务验收       |
+-------------------------------------------------------+
```

### 2.1 Agent Loop 改造前后的架构差异

改造前，DiCode 的 Agent Loop 主要围绕“模型调用—工具执行”运行，系统能够完成基本任务，但任务控制权较多依赖模型自身：错误是否被理解、失败后是否换策略、何时可以结束，都缺少独立的运行判断。

```text
改造前：模型主导的线性循环

用户输入
   |
   v
+--------+     +----------+     +----------+
| 模型   | --> | 工具解析 | --> | 工具执行 |
+--------+     +----------+     +----------+
    ^                                  |
    |                                  v
    +---------------------------- 工具结果

主要断点：
- 参数解析失败可能只写日志，没有形成模型可见的工具结果
- 通过“工具名 + 参数完全相同”识别重复，难以覆盖变形循环
- 历史、环境和工具输出持续累积，运行循环与上下文治理缺少协同
- 模型声明完成与任务实际完成之间缺少独立证据
```

改造后，Agent Loop 被拆分为运行控制、上下文构造和证据验证三个相互约束的部分。模型仍负责推理和选择行动，但不再独自承担错误恢复、进展判断和完成判断。

```text
改造后：Harness 控制的闭环 Agent

                     +------------------------+
                     | Context Engineering    |
                     | 目标、记忆、环境、摘要 |
                     | 错误与恢复提醒         |
                     +-----------+------------+
                                 |
                                 v
用户输入 --> [任务状态] --> [模型决策] --> [工具调用]
                ^                 |              |
                |                 |              v
                |          [结构化错误] <-- [校验与执行]
                |                 |              |
                +------ [进展判断与恢复] <-------+
                                 |
                                 v
                        [完成判断 / 明确阻塞]
                                 |
                                 v
                    [历史、生命周期、运行证据]
```

| 架构维度   | 改造前                                 | 改造后                                         |
| ---------- | -------------------------------------- | ---------------------------------------------- |
| 循环控制   | 模型决定下一步，系统主要负责转发       | Harness 管理迭代、执行、恢复和终止             |
| 错误闭环   | 部分解析错误停留在日志，模型可能看不到 | 无效调用也形成结构化结果，反馈模型并写入证据   |
| 循环识别   | 比较工具名和参数是否完全相同           | 判断任务意图、失败类别和是否产生新证据         |
| 恢复方式   | 依赖模型自我反思或用户提醒             | 连续无进展后，在下一决策点注入恢复要求         |
| 上下文供给 | 历史与完整环境反复累积                 | 按预算构造上下文，普通轮次只发送变化信息       |
| 任务状态   | 对话历史承担大部分状态职责             | 目标、Todo、短期状态和长期记忆分层管理         |
| 完成判断   | 较依赖模型自行声明完成                 | 结合工具失败、未完成事项和活动进程检查         |
| 问题定位   | 依赖零散日志，难以复原完整过程         | 保存模型历史、工具明细、生命周期和状态检查     |
| 质量验证   | 以单次体验和人工观察为主               | 通过固定案例、正式判分、真实任务和证据门槛验证 |

这次改造的本质，是把 Agent Loop 从“模型不断调用工具”，升级为“系统持续判断任务是否在正确推进”。模型能力因此被置于一个可恢复、可审计、可测评的运行闭环中。

## 3. Agent Harness：让任务可靠运行

Agent Harness 是包围模型推理循环的运行框架。它不负责替代模型思考，而是确保每一步输入、执行、反馈和终止都可控。

### 3.1 统一任务生命周期

Harness 统一管理：

```text
任务开始
   -> 迭代开始
   -> 模型请求
   -> 工具调用
   -> 工具结果
   -> 下一轮模型请求
   -> 完成 / 失败 / 取消
```

每个阶段都有明确状态和顺序，任务恢复后使用新的运行实例，不覆盖此前证据。

这使系统能够判断：是否存在模型请求未结束、工具调用未闭合、任务结束后仍继续执行等异常。

### 3.2 工具执行与错误反馈闭环

现场问题中，模型连续发出缺少参数的文件写入调用。旧路径在参数解析阶段直接丢弃调用，只打印错误日志：

```text
旧路径：无效调用 -> 解析失败 -> 打印日志 -> 模型重试
```

改造后：

```text
新路径：无效调用
          -> 禁止执行
          -> 生成结构化错误
          -> 反馈给模型
          -> 写入证据
          -> 进入进展判断
```

这补齐了 Harness 中最关键的反馈断点：系统记录错误、模型看到错误、下一步改变行为，三者必须属于同一闭环。

### 3.3 基于进展的循环恢复

旧重复检测主要比较工具名和参数是否完全相同，但真实死循环经常改变命令写法、参数顺序甚至工具名称。

新机制判断三个问题：

1. 当前行动服务于什么任务意图；
2. 本轮属于哪类失败；
3. 是否产生了新证据或状态变化。

它可以识别：

- 参数形式变化但仍重复同一失败假设；
- 权限、资源不存在、超时和输出截断；
- 工具成功但连续得到相同结果；
- 表面更换工具，实际没有缩小问题范围。

连续两次没有进展后，Harness 会在下一轮要求模型检查假设、验证参数、切换策略，或报告具体阻塞。

### 3.4 完成、取消与恢复

Harness 在接受完成前检查当前工具失败、空结果、未完成 Todo 和活动终端等状态。任务中断或恢复时，重新加载当前目标、步骤和短期工作状态。

当前完成检查仍以运行状态为主，尚未形成完整的目标级验收合同。这是下一阶段需要补齐的能力。

### 3.5 运行证据

Harness 为每个任务保存四类证据：

| 证据     | 内容                                     |
| -------- | ---------------------------------------- |
| 生命周期 | 任务、模型、工具、重试、完成的顺序和状态 |
| 运行明细 | 完整工具参数、结果和用户响应             |
| 状态检查 | 动作是否闭合、生命周期是否矛盾           |
| 模型历史 | 模型实际看到和产生的内容                 |

用户可以一键导出诊断文件。证据写入采用顺序追加和失败隔离，证据异常不会反过来中断用户任务。

## 4. Context Engineering：管理模型每一轮看到的信息

上下文工程不是简单“缩短聊天记录”，而是持续构造每一次模型请求所需的最小充分信息。

### 4.1 三种历史视图

```text
完整历史
|  保留全部事实，用于恢复、审计和回滚
|
+--> 模型上下文
|    当前真正发送给模型的内容
|
`--> 界面记录
     用户看到的完整交互过程
```

压缩只改变模型上下文，不覆盖完整历史。这样既能减少模型用量，又不会破坏恢复和审计。

### 4.2 分阶段上下文治理

```text
计算当前用量
   |
   v
清理过期工具输出
   |
   v
重新计算用量 ---- 已恢复 ----> 继续任务
   |
   | 仍有压力
   v
保留最近完整对话
   |
   v
结构化摘要旧历史
   |
   v
重新注入关键任务状态
   |
   v
检查工具调用完整性
   |
   v
发送模型
```

结构化摘要重点保留：

- 当前目标和用户约束；
- 已修改及仍需处理的文件；
- 已作出的决策；
- 失败和未完成工作；
- 尚未结束的工具状态；
- 继续执行所需的精确信息。

### 4.3 工具状态保护

工具调用和执行结果必须成组保留，不能为了减少上下文而拆散。对失败工具、未完成工具和需要继续处理的自定义工具，系统使用独立保护区保存精确信息，而不是只依赖摘要模型转述。

这解决了上下文压缩中最难的问题：压缩后不仅要“读起来合理”，还必须能继续执行。

### 4.4 长任务行为提醒

长任务中，早期指令会逐渐远离模型当前注意区域。Harness 与上下文工程协同，在真正发生连续无进展时，把简短、具体的恢复要求注入下一轮，而不是每轮重复一大段静态规则。

```text
初始规则：说明总体边界
运行证据：判断当前发生了什么
即时提醒：在决策点告诉模型下一步必须改变什么
```

### 4.5 环境上下文增量化

过去普通轮次会重复发送操作系统、运行模式、成本和完整目录树。现在：

- 任务开始、恢复或压缩边界发送完整环境；
- 普通轮次只发送编辑器变化、终端增量和最近修改；
- 没有变化时不重复发送；
- 取消周期性重复发送完整目录树。

### 4.6 长短期记忆

短期记忆属于当前任务上下文，保存目标、步骤、Todo、当前进度和召回来源。

长期记忆用于跨任务连续性，但必须由用户确认。每次模型请求前重新检查有效性，已撤销内容不会继续注入。

```text
当前任务状态 -----------------> 短期记忆
用户确认的规则与经验 ----------> 长期记忆
外部文件、Git、知识库 ----------> 按需查询的事实源
```

三者权威性不同，不能混为同一份“模型记忆”。

### 4.7 压缩防抖与回退

上下文工程也可能失败：摘要无效、压缩收益不足、短时间重复触发。系统因此增加：

- 压缩后重新计量；
- 最低收益判断；
- 尝试次数上限；
- 冷却和防抖；
- 失败时安全回退。

所有随任务长度增长的资源，都必须有预算或上限。

## 5. Harness 与 Context Engineering 如何协同

两者不是独立功能，而是在每一轮任务中互相驱动。

```text
Harness 记录工具失败
        |
        v
Context Engineering 把错误和恢复要求放入下一轮
        |
        v
模型选择新的行动
        |
        v
Harness 执行工具并判断是否产生进展
        |
        +---- 有进展 ----> 更新短期状态，继续任务
        |
        `---- 无进展 ----> 再次恢复或报告阻塞
```

| Harness 负责             | Context Engineering 负责       |
| ------------------------ | ------------------------------ |
| 任务何时开始、结束或取消 | 模型本轮需要看到哪些信息       |
| 工具是否可以执行         | 哪些旧工具输出可以裁剪         |
| 错误如何形成结构化结果   | 错误和恢复要求如何进入下一轮   |
| 是否产生实际进展         | 如何保留目标、约束和未完成状态 |
| 生命周期和证据如何落盘   | 如何控制上下文用量和压缩频率   |

如果只有 Harness，没有上下文工程，任务会因信息膨胀而退化；如果只有上下文工程，没有 Harness，错误和工具状态就无法可靠反馈和执行。

## 6. 测评 Harness

产品运行框架之外，本次还建设了两套测评运行框架，用来验证改造是否真实有效。

### 6.1 Agent 测评

```text
固定代码任务
   -> 隔离运行 DiCode
   -> 收集补丁与过程证据
   -> 官方工具判分
   -> 汇总任务完成率、工具准确率、死循环率
```

主要建设包括：

- 固定 50 个公开代码修复案例，选取过程不查看答案；
- 限制每个案例的时间、算力和模型请求次数；
- 不以 Agent 自称完成为准；
- 支持分批运行、失败续跑和最终汇总；
- 拒绝案例缺失、重复、版本混用和证据不完整的数据；
- 使用置信区间提示小样本的不确定性。

### 6.2 上下文压缩测评

```text
                 +--> 新方案 -->+
同一任务历史 ----|              |--> 自动检查 --> 人工盲审 --> 报告
                 +--> 旧方案 -->+
```

主要建设包括：

- 新旧方案使用完全相同的输入；
- 检查信息保留、工具状态、压缩收益和任务连续性；
- 支持固定语料、自动回放、真实任务和人工盲审；
- 使用哈希锁定测试资产，防止中途修改标准；
- 支持中断恢复和可追溯报告。

这两套测评 Harness 将“感觉更好”转化为“同一条件下可复现、可比较”。

## 7. 工程工作量与验证

### 7.1 交付规模

| 维度       | 结果                                           |
| ---------- | ---------------------------------------------- |
| 上下文工程 | 完成 9 个阶段的设计、实现、测试和验收          |
| 产品能力   | 恢复、证据、记忆、压缩、环境降噪               |
| 测评体系   | Agent 与上下文两套独立运行框架                 |
| Agent 主测 | 固定 50 个案例及正式证据门槛                   |
| 验证方式   | 单元测试、同输入对比、自动回放、盲审、真实任务 |

### 7.2 真实任务验证

真实任务连续两次发出“文件写入缺少必要参数”：两次调用均被安全拦截并记录；第二次失败后，第三轮收到恢复要求；Agent 随后停止重复并正常完成。工具调用、错误结果、恢复要求和最终状态均已落盘。

### 7.3 自动验证

| 验证范围                       | 结果                         |
| ------------------------------ | ---------------------------- |
| 当前 Agent、环境与任务聚焦回归 | 95 项通过，4 项按计划跳过    |
| 类型检查                       | 通过                         |
| 可发布扩展构建                 | 通过                         |
| 现有接口兼容                   | 未改变                       |
| 上下文真实任务验收             | 通过                         |
| 上下文工程收束回归             | 6308 项通过，58 项按计划跳过 |

## 8. 架构层面的关键结论

### 结论一：Agent 的可靠性取决于 Harness，而不只取决于模型

模型负责提出行动，Harness 负责保证行动能够安全执行、结果能够正确反馈、循环能够终止。模型错误不可避免，关键是系统能否吸收并纠正错误。

### 结论二：上下文是预算，不是无限缓存

每一段重复环境、每一个旧工具输出，都会在后续模型请求中持续付费。上下文应按价值分层管理，而不是等到超限后一次性删除。

### 结论三：恢复应发生在决策点

长任务中重复强调初始规则效果有限。基于当前失败证据，在下一次行动前给出简短、具体的恢复要求，更容易改变行为。

### 结论四：判断进展比判断重复更重要

调用形式可以变化，但任务可能仍原地踏步。可靠恢复必须关注新证据、状态变化和问题是否收敛。

### 结论五：测评 Harness 是产品架构的一部分

没有固定任务、正式判分和证据门槛，就无法判断架构改造是否有效，也无法形成稳定的发布标准。

## 9. 当前边界与下一步

当前恢复机制通过强提示推动 Agent 改变策略，尚未对所有高风险重复行为实施硬阻断。完成检查也仍以运行状态为主，尚缺完整的目标级验收条件和证据判定。

Agent 测评框架和 50 个主测案例已经固定，小规模真实案例已验证运行与判分链路；正式 KPI 仍需等待全部案例完成官方判分。

下一阶段重点：

1. 建立任务开始时的目标级验收条件；
2. 用证据判断用户目标是否真正完成；
3. 对高风险重复行为增加硬阻断；
4. 完成 50 个主测案例，形成正式质量基线；
5. 持续跟踪循环率、恢复率、模型用量和任务成功率。

## 10. 总结

本次改造以 Agent Harness 和 Context Engineering 为核心，重构了 DiCode 模型之外的关键能力：Harness 负责让任务正确运行和恢复，上下文工程负责让模型在每一轮看到正确且足够的信息，证据与测评框架负责证明改造是否有效。

这使 DiCode 从依赖用户提醒纠错的执行工具，进一步具备支撑长任务、规模化质量治理和持续演进的 Agent 架构基础。

## 架构参考

- [Building Effective AI Coding Agents for the Terminal: Scaffolding, Harness, Context Engineering, and Lessons Learned](https://arxiv.org/abs/2603.05344)
